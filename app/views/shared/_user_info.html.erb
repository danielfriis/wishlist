<% user ||= @user %>

  <a href="<%= user_path(user) %>">
    <%= image_tag avatar(user, :large), alt: user.name, class: "gravatar gravatar-large img-rounded" %>
  </a>

  <h1><%= link_to user.name, user, class: "no-color" %></h1>
  <span>
    <% if user.location? %>
      <i class="icon-map-marker"></i> <%= user.location %> 
    <% end %> 
    <% if user.gender? %> 
      <i class="icon-user"></i> <%= user.gender %>
    <% end %>
  </span>
  <span>
    <%= render partial: 'users/follow_form', locals: {:user => user} if signed_in? %>
    <%= render partial: 'users/follow_stats', locals: {:user => user} %>
  </span>

<!-- <a href="#" class="btn">Follow</a> -->
