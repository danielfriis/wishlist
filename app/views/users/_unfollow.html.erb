<% subject ||= @subject %>
<%= form_for(current_user.relationships.find_by_followed_id_and_followed_type(subject.id, subject.class.name),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Following", class: "btn btn-primary" %>
<% end %>